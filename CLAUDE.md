# STAI-X 2026 SC01 guest segment: repo orientation

This repo is a **15-minute conference talk delivered as Jupyter notebooks**, plus the material
behind it. Read this before editing anything here.

## What this is

- **Venue:** STAI-X 2026, short course SC01, *Agentic AI: From Zero to Infinity*
  (primary instructor Tian Zheng, Columbia). Boston, Friday July 31 2026, 8:00-12:00.
  Shihao Yang is a **guest speaker**, not an instructor: 15 minutes plus 5 minutes Q&A.
- **Title:** "Out-of-the-Box Agentic AI for Research and Education."
- **Brief from the organizer:** how to use agentic AI such as Claude Code in daily task
  workflows, how to get started and hit the ground running, two use cases, lessons learned.
- **Audience:** statisticians. Mostly no prior agent experience. They are working in
  Codespaces during the course.

## Deliberate design decisions (do not undo these without asking)

- **No slides.** Notebooks with rendered markdown replace the deck. This was an explicit
  choice: the notebook shows prompt, code, and result together, which is the talk's thesis.
- **Notebooks are executed and committed with outputs.** Figures and tables must be visible
  on GitHub without running anything. If you edit a code cell, re-execute the notebook.
- **Results are real and reported honestly.** ARGO beats the AR(3) benchmark by only ~2.5%
  in RMSE here. That modest number is *the teaching point* and must not be inflated. The
  original 2015 paper used weekly US flu data against millions of queries; this is monthly
  dengue with four terms and 96 observations, so a small margin is expected.
- **No student data.** The roster in notebook 02 is fabricated with the real structure and
  none of the real people. The actual reading group has ~28 named students; their names and
  addresses must never enter this public repo.
- **`logs/` is gitignored** and may contain personal context from the authoring sessions.

## Layout

```
notebooks/00_start_here.ipynb              the talk itself (framing, setup, lessons)
notebooks/01_research_dengue.ipynb         use case 1, executed with figures
notebooks/02_education_reading_group.ipynb use case 2, executed
prompts/                                   every prompt as copy-pasteable text
docs/lessons-learned.md                    long-form lessons from SISMID 2026
scripts/                                   secret-encrypt.sh, claude-login.sh (only these two)
secrets/                                   encrypted class credential + its README
data/MX_Dengue_trends.csv                  Mexico dengue cases + Google search, 2004-2011
```

## Rebuilding the notebooks

The notebooks are generated by `build_*_nb.py` scripts (gitignored scaffolding, kept locally)
and then executed:

```bash
.venv/bin/python build_research_nb.py
.venv/bin/jupyter nbconvert --to notebook --execute --inplace notebooks/01_research_dengue.ipynb
```

If you only need a small text fix, edit the `.ipynb` directly rather than regenerating.

## Source material this was distilled from

- `~/Workspace/sismid2026/` is the SISMID 2026 short course. `agentic-ai-teaching-lessons.md`
  (in Chinese) is the primary lessons-learned source; `docs/lessons-learned.md` here is the
  English port aimed at this different audience.
- `~/Workspace/sismid2026/course-repo/` is the SISMID course repo, source of the dengue data
  and the two-lane (prompt / worked-solution) notebook pattern.
- `~/Workspace/canvas/` holds hand-written 2024 Canvas API scripts (`move_date.py`, `quizzes.py`)
  used as the "before" in the education notebook.

## Writing conventions

- **No em dashes** in any English prose here (a standing preference of the author).
- Prose over bullet fragments in the notebooks. These are read aloud and read later.
- Do not oversell. The talk's credibility rests on the honest 2.5%.
